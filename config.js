const config = {
    uname: "root",
    upass: "", //blank for windows, root for mac, unless changed
    port: 3306, //3306 windows, 8889 for mac
    host: "localhost",
    db: "hackathon"
}

module.exports = config;